# Create the variable for the region name
variable "region" {
  type    = string
  default = "eu-north-1"
}

# Create the variable for the general name
variable "service_name" {
  type    = string
  default = "am-machine"
}
